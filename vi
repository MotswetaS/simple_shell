#include "shell.h"

/*
 * function - my_shell_write
 * writes a character string to the terminal
 * my_write is a pointer
 */

void my_shell_write(const char *my_write)
{
	write(STDOUT_FILENO, my_write, strlen(my_write));
}
